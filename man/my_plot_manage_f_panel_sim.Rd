% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/my_plot_manage_f_panel_sim.R
\name{my_plot_manage_f_panel_sim}
\alias{my_plot_manage_f_panel_sim}
\title{Absolute F\link{t} panel with Fmsy band, scaled ribbons, and scenarios}
\usage{
my_plot_manage_f_panel_sim(
  rep_man,
  scenario_color = NULL,
  show_CIs = TRUE,
  CI = 0.95,
  show_legend = FALSE
)
}
\arguments{
\item{rep_man}{A fitted SPiCT report object (\code{spictcls}), optionally with \code{$man}.}

\item{scenario_color}{Optional named character vector for scenario colours; if \code{NULL},
a repeatable internal palette is used.}

\item{show_CIs}{Logical; draw CI bands/edges where applicable. Default \code{TRUE}.}

\item{CI}{Confidence level in (0, 1). Default \code{0.95}.}

\item{show_legend}{Logical; show scenario legend when scenarios exist. Default \code{FALSE}.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots absolute fishing mortality \eqn{F_t} with an Fmsy reference band
(constant or time-varying), plus ribbons derived from \eqn{(F/F_{MSY})\times F_{MSY}}.
If management scenarios exist in \code{rep_man$man}, their trajectories are
overlaid with a stable, repeatable colour mapping. For raw fits (no scenarios),
in-sample \eqn{F_t} is solid and one-step-ahead is dotted; a bridge segment is drawn.
When simulation truth is available in \code{rep_man$inp$true}, the true
\eqn{F_t} path and horizontal Fmsy are overlaid in semi-transparent orange,
matching \strong{plotspict} styling.
}
\details{
\itemize{
\item Fmsy handling supports both constant and time-varying growth configurations.
\item Sub-annual \code{Fs} is drawn (semi-transparent) when \code{dtc < 1}.
\item Vertical management lines are added using \code{inp$maninterval} when present,
otherwise a line at the last observation time is drawn.
}
}
\examples{
\dontrun{
p <- my_plot_manage_f_panel_sim(fit)
print(p)
}

}
