% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_hindcast_grid_many.R,
%   R/plot_hindcast_grid_scenario123.r
\name{plot_hindcast_grid_many}
\alias{plot_hindcast_grid_many}
\title{Hindcast grid for many scenarios (batch)}
\usage{
plot_hindcast_grid_many(all_models, scenarios = NULL, ...)

plot_hindcast_grid_many(all_models, scenarios = NULL, ...)
}
\arguments{
\item{all_models}{Named list-of-lists by scenario (see
\code{\link[=plot_hindcast_grid_scenario123]{plot_hindcast_grid_scenario123()}} for expected inner names).}

\item{scenarios}{Optional character vector of scenario names to plot. If
\code{NULL}, all names in \code{all_models} are used.}

\item{...}{Passed through to \code{\link[=plot_hindcast_grid_scenario123]{plot_hindcast_grid_scenario123()}} (e.g.,
\code{npeels}, \code{peel.dtc}, \code{CI}, \code{add.mase}, \code{save}, \code{out_dir}, \code{width},
\code{height}, \code{dpi}, etc.).}

\item{scenario_names}{Character vector of scenarios to process; default \code{names(all_models)}.}
}
\value{
Invisibly, a named list of results (one per scenario).

A named list of patchwork objects, one per scenario (invisible).
}
\description{
Iterates scenarios in \code{scenario_names} (default: all found in \code{all_models})
and writes one 2×3 grid PNG per scenario using
\link[=plot_hindcast_grid_scenario]{plot_hindcast_grid_scenario()}.

Convenience wrapper that loops over scenarios and calls
\code{\link[=plot_hindcast_grid_scenario123]{plot_hindcast_grid_scenario123()}} for each one. Returns a named list of
patchwork objects (invisible) and lets each call save its PNG if requested.
}
\examples{
\dontrun{
# Batch scenarios S1–S4:
res <- plot_hindcast_grid_many(
  all_models,
  scenarios = paste0("S", 1:4),
  npeels = 2, peel.dtc = FALSE,
  CI = 0.95, add.mase = TRUE, verbose = FALSE,
  show_legends = TRUE, save = TRUE,
  out_dir = file.path("FIG", "Hindcast"),
  width = 14, height = 6, dpi = 300
)
}
}
\seealso{
\link[=plot_hindcast_grid_scenario]{plot_hindcast_grid_scenario()}.

plot_hindcast_grid_scenario123
}
