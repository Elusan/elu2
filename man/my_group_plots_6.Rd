% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/my_group_plots_6.R
\name{my_group_plots_6}
\alias{my_group_plots_6}
\title{ELU2: 2×3 summary grid from existing panels (ggplot2)}
\usage{
my_group_plots_6(
  x,
  save = FALSE,
  out_dir = "FIG",
  filename = NULL,
  width = 20,
  height = 9,
  dpi = 300,
  show_legend_biomass = TRUE,
  show_legend_f = FALSE,
  show_legend_catch = FALSE,
  show_legend_bbmsy = TRUE,
  show_legend_ffmsy = FALSE,
  guides_collect = FALSE
)
}
\arguments{
\item{x}{A fitted SPiCT report object (like \code{m1}) or a management object
returned by \code{manage()} (like \code{rman}). The same object will be passed to
all six panel functions.}

\item{save}{Logical; if \code{TRUE}, saves the composed grid to \code{out_dir/filename}.
Default \code{FALSE}.}

\item{out_dir}{Directory where the image is written when \code{save = TRUE}.
Default \code{"FIG"}.}

\item{filename}{Output filename when \code{save = TRUE}. If \code{NULL}, a name is
generated automatically using the object class and current time.}

\item{width, height, dpi}{Device width/height (inches) and resolution for saving.
Defaults: \code{width = 20}, \code{height = 9}, \code{dpi = 300}.}

\item{show_legend_biomass}{Show legend in the biomass panel?
Default \code{TRUE} (matches your example call).}

\item{show_legend_f}{Show legend in the F panel? Default \code{FALSE}.}

\item{show_legend_catch}{Show legend in the catch panel? Default \code{FALSE}.}

\item{show_legend_bbmsy}{Show legend in the B/Bmsy panel? Default \code{TRUE}.}

\item{show_legend_ffmsy}{Show legend in the F/Fmsy panel? Default \code{FALSE}.}

\item{guides_collect}{If \code{TRUE}, use patchwork legend collection.
Default \code{FALSE} to avoid altering your panel legend behavior.}
}
\value{
Invisibly returns the patchwork object (and, if \code{save = TRUE}, the file path as an attribute \code{"file"}).
}
\description{
Builds a 2×3 grid from your six existing ELU2/SPiCT ggplot panels and prints it.
It accepts either a fitted model (e.g., \code{m1}) or a management object
produced by \code{manage()} (e.g., \code{rman}) and \strong{does not} change any of the
internal settings of your panel functions—those are called as-is.

Panels (left→right, top→bottom):
\enumerate{
\item \code{my_plot_manage_biomass_panel()}
\item \code{my_plot_manage_f_panel()}
\item \code{my_plot_manage_catch_panel()}
\item \code{my_plot_manage_bbmsy_panel()}
\item \code{my_plot_manage_ffmsy_panel()}
\item \code{my_plot_kobe_all_management_scenario()}
}
}
\examples{
\dontrun{
# Model object:
g1 <- my_group_plots_6(m1, save = TRUE, out_dir = "FIG/KobePhasesNEW")

# Management object:
g2 <- my_group_plots_6(rman)
}

}
