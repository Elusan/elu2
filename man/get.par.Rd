% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parameters.R, R/utility.R
\name{get.par}
\alias{get.par}
\alias{list.quantities}
\title{Extract parameter estimates and uncertainty from a SPiCT model result}
\usage{
get.par(
  parname,
  rep = rep,
  exp = FALSE,
  random = FALSE,
  fixed = FALSE,
  CI = 0.95
)

get.par(
  parname,
  rep = rep,
  exp = FALSE,
  random = FALSE,
  fixed = FALSE,
  CI = 0.95
)

list.quantities(rep)
}
\arguments{
\item{parname}{Character string containing the name of the variable of
interest.}

\item{rep}{A result report as generated by running \code{fit.spict}.}

\item{exp}{Take exp of the variable? TRUE/FALSE.}

\item{random}{DUMMY not used anymore. (Is the variable a random effect?
TRUE/FALSE.)}

\item{fixed}{DUMMY not used anymore. (Is the variable a fixed effect?
TRUE/FALSE.)}

\item{CI}{Confidence intervals to be calculated, e.g. 0.9 for the 90\\%
confidence intervals. By default (CI = 0.95), the 95\\% confidence
intervals are estimated.}

\item{rep_obj}{A fitted SPiCT model object (i.e., the output of \code{fit.spict()}).}
}
\value{
A matrix with columns: lower bound, estimate, upper bound, standard deviation, and coefficient of variation.
If \code{parname} is time-varying (e.g., \code{"logB"}, \code{"logF"}), row names will be the time vector.

get.par returns a matrix with four columns containing respectively:
1) the lower 95\\% confidence limit; 2) the parameter estimate; 3) the
upper 95\\% confidence limit; 4) the parameter standard deviation in the
domain it was estimated (log or non-log). \code{list.quantities} returns a
vector with the names of all estimated parameters and derived quantities.
}
\description{
Extracts the estimate, standard deviation, confidence intervals, and coefficient of variation
for a specified parameter from a fitted SPiCT result object.
}
\details{
\code{get.par} is a helper function for extracting the value and
uncertainty of a specific model parameter, random effect or derived
quantity. \code{list.quantities} gives the names of all quantities.
}
\examples{
\dontrun{
get.par("logBmsy", rep_obj, exp = TRUE)
}

## Run the South Atlantic albacore assessment
data(pol)
rep <- fit.spict(pol$albacore)

## See all quantitites that can be extracted
list.quantities(rep)

## Extract the Bmsy reference point
Bmsy <- get.par('logBmsy', rep, exp=TRUE)

## Extract the exploitable biomass estimates
Best <- get.par('logB', rep, exp=TRUE)

## Extract the estimated carrying capacity
K <- get.par('logK', rep, exp=TRUE)
}
