% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_all_retro.R
\name{plot_all_retro}
\alias{plot_all_retro}
\title{Batch plot retrospective diagnostics for multiple SPiCT models}
\usage{
plot_all_retro(
  models_retro,
  output_dir = "FIG/",
  peel_colors = NULL,
  palette = "JABBA",
  width = 6,
  height = 4,
  dpi = 400,
  prefix = "retrospective_"
)
}
\arguments{
\item{models_retro}{A named list of fitted SPiCT models containing retrospective output (\verb{$retro}).}

\item{output_dir}{Directory where the plots should be saved. Created if it doesn't exist.
Default is \code{"FIG/"}.}

\item{peel_colors}{Optional vector of peel colors. If NULL, defaults to \code{cols()} output.}

\item{palette}{Character string specifying the palette name, currently unused in this function,
but available for future customization.}

\item{width}{Width of the saved plots in inches. Default is 6.}

\item{height}{Height of the saved plots in inches. Default is 4.}

\item{dpi}{Resolution in dots per inch for the saved PNG. Default is 400.}

\item{prefix}{Filename prefix used when saving plots. Default is \code{"retrospective_"}.}
}
\value{
Invisibly returns \code{NULL}. Side effect: saves .png plots to \code{output_dir}.
}
\description{
Loops over a named list of fitted SPiCT models with retro components and generates
retrospective plots using \code{retro.rho.ndiaye2()}. Saves each plot as a high-resolution
PNG file. Optionally allows custom peel colors or palette choice.
}
\details{
This function is designed to automate the plotting of retrospective diagnostics
(B, F, B/Bmsy, F/Fmsy) for multiple SPiCT model fits. Each plot is generated using
a call to \code{retro.rho.ndiaye2()}, and saved with a filename that includes the
model's name.

The function checks for the presence of \verb{$retro} in each model and skips models
that are missing or have NULL retro outputs. Errors in plotting individual models
are caught and reported without stopping the loop.
}
\examples{
\dontrun{
models <- list(S1F = model_fox, S1P = model_pella, S1S = model_schaefer)
plot_all_retro(models, output_dir = "FIG/")
}

}
\seealso{
\code{\link{retro.rho.ndiaye2}}, \code{\link{ggsave}}, \code{\link{cols}}
}
