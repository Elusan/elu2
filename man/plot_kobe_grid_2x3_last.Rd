% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_kobe_grid_2x3_last.R
\name{plot_kobe_grid_2x3_last}
\alias{plot_kobe_grid_2x3_last}
\title{2×3 Kobe grid (rows = data type; cols = model) + in-panel tags + optional save}
\usage{
plot_kobe_grid_2x3_last(
  models_list,
  man.legend = FALSE,
  text_size = 4,
  save = TRUE,
  out_dir = "FIG/KobePhasesNEW",
  width = 20,
  height = 9,
  dpi = 300,
  file_basename = NULL,
  ...
)
}
\arguments{
\item{models_list}{Named list for one scenario containing up to six model fits:
SxP.SDM, SxS.SDM, SxF.SDM, SxP.GLM, SxS.GLM, SxF.GLM (names are used).}

\item{man.legend}{Logical; passed to \code{my_plot_kobe_all_management_scenario()}.}

\item{text_size}{Numeric; size of the in-panel tag text.}

\item{save}{Logical; if TRUE, saves a PNG to \code{out_dir}.}

\item{out_dir}{Character; output directory for the PNG. Created if missing.}

\item{width, height}{Numeric; device size in inches for saving.}

\item{dpi}{Numeric; resolution for saving.}

\item{file_basename}{Optional base name (without extension). If NULL, derived
from the first list name (e.g., "S1_KobeGrid_2x3").}

\item{...}{Extra args forwarded to \code{my_plot_kobe_all_management_scenario()}.}
}
\value{
Invisibly returns a list with elements:
\item{plot}{the patchwork object}
\item{file}{path to the saved file (or NULL if not saved)}
}
\description{
Builds a 2×3 grid of Kobe panels using your existing
\code{my_plot_kobe_all_management_scenario()}:
Row 1 = SDM (Pella, Schaefer, Fox)
Row 2 = GLM (Pella, Schaefer, Fox)

Each cell shows a small bold tag at the top-left with the original list name
(e.g., "S1P.SDM"). No plot titles are added.
}
