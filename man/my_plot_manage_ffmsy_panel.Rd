% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/my_plot_manage_ffmsy_panel.R
\name{my_plot_manage_ffmsy_panel}
\alias{my_plot_manage_ffmsy_panel}
\title{F/Fmsy panel (ggplot2) with optional scenario overlays}
\usage{
my_plot_manage_ffmsy_panel(
  rep_man,
  scenario_color = NULL,
  show_CIs = TRUE,
  CI = 0.95,
  show_legend = FALSE
)
}
\arguments{
\item{rep_man}{A fitted SPiCT report (\code{spictcls}) or a \code{rep$man} list.}

\item{scenario_color}{Optional named character vector of colours to use; if \code{NULL}, uses man_cols().}

\item{show_CIs}{Logical; if \code{TRUE}, draws base-fit CI ribbon & bounds. Default \code{TRUE}.}

\item{CI}{Confidence level in (0, 1). Default \code{0.95}.}

\item{show_legend}{Logical; show scenario legend when scenarios exist. Default \code{FALSE}.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots the historical F/F_MSY trajectory with optional confidence ribbons.
Accepts either a full fitted \code{spictcls} (with or without \verb{$man}) OR a raw
\code{rep$man} list of scenarios. Colours and order are kept unchanged.

Behavior (fitted-only, no scenarios):
\itemize{
\item Base line (F/Fmsy) is BLUE and SOLID up to last observed time.
\item From the vertical line to the right, BLUE becomes DOTTED.
\item CI ribbon/lines are continuous across the vertical line (no gap).
}
}
