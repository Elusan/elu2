% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel-kobe.R, R/plot_elu2_panel_kobe.R
\name{plot_elu2_panel_kobe}
\alias{plot_elu2_panel_kobe}
\title{Kobe panel (complete, ggplot2) — OSA on, MSY ellipse, scenarios, E(B∞) mini-legend}
\usage{
plot_elu2_panel_kobe(
  model,
  logax = FALSE,
  plot.legend = TRUE,
  man.legend = TRUE,
  ext = TRUE,
  rel.axes = FALSE,
  xlim = NULL,
  ylim = NULL,
  labpos = c(1, 1),
  xlabel = NULL,
  stamp = NULL,
  verbose = TRUE,
  CI = 0.95,
  print_it = FALSE
)

plot_elu2_panel_kobe(
  model,
  logax = FALSE,
  plot.legend = TRUE,
  man.legend = TRUE,
  ext = TRUE,
  rel.axes = FALSE,
  xlim = NULL,
  ylim = NULL,
  labpos = c(1, 1),
  xlabel = NULL,
  stamp = NULL,
  verbose = TRUE,
  CI = 0.95,
  print_it = FALSE
)
}
\arguments{
\item{model}{A fitted \code{spictcls} object (\code{fit.spict()}) with
\code{model$inp$reportmode == 0}.}

\item{logax}{Logical; use log10 scales for both axes. Default \code{FALSE}.}

\item{plot.legend}{Logical; draw the compact mini-legend
(\eqn{E(B_\infty)} and \strong{True}). Default \code{TRUE}.}

\item{man.legend}{Logical; show scenario legend when \code{model$man} exists.
Default \code{TRUE}.}

\item{ext}{Logical; add secondary relative axes when plotting absolute axes.
Ignored when \code{rel.axes = TRUE}. Default \code{TRUE}.}

\item{rel.axes}{Logical; plot relative axes \eqn{B_t/B_{MSY}} vs \eqn{F_t/F_{MSY}}.
Forces \code{ext = FALSE}. Default \code{FALSE}.}

\item{xlim, ylim}{Optional numeric length-2 limits for x and y. Computed if \code{NULL}.}

\item{labpos}{Numeric length-2; reserved for future label placement (currently unused).}

\item{xlabel}{Optional x-axis label override (expression or character).}

\item{stamp}{Optional character stamp (currently unused).}

\item{verbose}{Logical; show extraction fallbacks warnings. Default \code{TRUE}.}

\item{CI}{Confidence level for intervals (used in internal extractors). Default \code{0.95}.}

\item{print_it}{Logical; if \code{TRUE}, also prints the ggplot. Default \code{FALSE}.}
}
\value{
A \code{ggplot} object.

A \code{ggplot} object representing the Kobe panel.
}
\description{
Produces a Kobe-style phase plot either in absolute units (\eqn{B_t} vs \eqn{F_t})
with secondary relative axes, or directly in relative units (\eqn{B_t/B_{MSY}} vs
\eqn{F_t/F_{MSY}}). Fills quadrants, draws the MSY ellipse, historical trajectory,
optional prediction segment to \eqn{E(B_\infty)}, scenario trajectories (if
\code{rep$man} present), and a compact \eqn{\\bold{E(B_{\\infty})}} mini-legend.
True trajectories are not drawn; the true reference point is shown if available.

Draws a phase plot of biomass vs. fishing mortality (absolute or relative to
MSY), including the MSY ellipse, historical and predicted trajectories,
optional management scenarios, and a compact, left-aligned mini-legend that
shows \eqn{E(B_\infty)} and the \strong{True} MSY marker when available.
}
\details{
The function supports:
\itemize{
\item Absolute axes (\eqn{B_t}, \eqn{F_t}) with optional secondary relative axes.
\item Relative axes (\eqn{B_t/B_{MSY}}, \eqn{F_t/F_{MSY}}) via \code{rel.axes = TRUE}.
\item Annual averaging when sub-annual time steps are present.
\item Optional scenario overlays from \code{model$man} (solid in-management,
dashed pre-management).
\item A gold diamond for \eqn{E(B_\infty)} and an orange triangle (not in legend)
for the true MSY point if \code{model$inp$true} exists.
}
}
\section{Dependencies}{

Uses \pkg{ggplot2} geoms/scales and base \pkg{stats}/\pkg{grDevices}.
If \pkg{ellipse} is available, the MSY ellipse is drawn; otherwise a point
at \eqn{(B_{MSY}, F_{MSY})} is shown.
}

\examples{
\dontrun{
m1 <- all_models$S1$S1P.SDM
plot_elu2_panel_kobe(m1, rel.axes = TRUE)
}
\dontrun{
p <- plot_elu2_panel_kobe(fit, rel.axes = TRUE)
print(p)
}

}
\seealso{
Other ELU2-panels: 
\code{\link{plot_elu2_panel_bbmsy}()},
\code{\link{plot_elu2_panel_biomass}()},
\code{\link{plot_elu2_panel_catch}()},
\code{\link{plot_elu2_panel_f}()},
\code{\link{plot_elu2_panel_ffmsy}()}
}
\concept{ELU2-panels}
