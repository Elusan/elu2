% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_all_scenarios_dynamic.R
\name{plot_all_scenarios_dynamic}
\alias{plot_all_scenarios_dynamic}
\title{Plot Prior–Posterior Grids for Any Number of Scenarios}
\usage{
plot_all_scenarios_dynamic(
  all_models,
  outdir = "FIG",
  file_prefix_prefix = "elu_prior_grid_",
  dpi = 300,
  autosize = TRUE,
  base_width_per_col = 2.8,
  base_height_per_row = 2.6,
  min_width = 10,
  min_height = 6,
  fixed_width = 12,
  fixed_height = 11
)
}
\arguments{
\item{all_models}{Named list of scenarios; each element is a named list
of fitted models for that scenario.}

\item{outdir}{Output directory for PNG files (passed to your grid function).}

\item{file_prefix_prefix}{Prefix used before the scenario name to compose the
filename stem.}

\item{dpi}{Output DPI passed through to your grid function.}

\item{autosize}{Logical; if \code{TRUE}, width/height are inferred from the
number of columns/rows. If \code{FALSE}, use \code{fixed_width}/\code{fixed_height}.}

\item{base_width_per_col}{Inches per prior-column when \code{autosize=TRUE}.}

\item{base_height_per_row}{Inches per model-row when \code{autosize=TRUE}.}

\item{min_width}{Minimum width (inches) when \code{autosize=TRUE}.}

\item{min_height}{Minimum height (inches) when \code{autosize=TRUE}.}

\item{fixed_width}{Fixed width (inches) when \code{autosize=FALSE}.}

\item{fixed_height}{Fixed height (inches) when \code{autosize=FALSE}.}
}
\value{
\code{invisible(NULL)} (files are saved by your existing function).
}
\description{
Iterates over \code{all_models} (a named list of scenarios), and for each
scenario calls your existing \code{elu_prior_posterior_grid_999()} to build
the grid. It automatically:
\itemize{
\item Works with \strong{any} number of scenarios and models (no fixed counts).
\item Skips scenarios where no model has active priors.
\item Repairs missing/zeroed \code{inp$priorsuseflags} using the names
returned by \code{priors_as_named_list()}.
\item Optionally autosizes the output figure based on rows/cols; you can
keep fixed size by setting \code{autosize = FALSE}.
}
Your functions \code{theme_minimal_compact_this()} and
\code{elu_prior_posterior_grid_999()} are \emph{not modified}.
}
\examples{
\dontrun{
plot_all_scenarios_dynamic(
  all_models = all_models,
  outdir = "FIG",
  file_prefix_prefix = "elu_prior_grid_",
  dpi = 300
)
}
}
