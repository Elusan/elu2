% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_all_scenario_KOBE_grids.R
\name{build_all_scenario_KOBE_grids}
\alias{build_all_scenario_KOBE_grids}
\title{Build a Kobe plot grid for a single scenario}
\usage{
build_all_scenario_KOBE_grids(
  all_models,
  scenario_name,
  layout = c("3x2", "3x1"),
  logax = FALSE,
  plot.legend = TRUE,
  man.legend = TRUE,
  ext = TRUE,
  rel.axes = FALSE,
  xlim = NULL,
  ylim = NULL,
  xlabel = NULL,
  stamp = NULL,
  verbose = TRUE,
  CI = 0.95
)
}
\arguments{
\item{all_models}{Named list of scenarios; each scenario is a named list of
fitted SPiCT objects (e.g., \code{"S1P.SDM"}, \code{"S1S.GLM"}).}

\item{scenario_name}{Character; scenario to plot (must exist in \code{all_models}).}

\item{layout}{Either \code{"3x2"} (default) or \code{"3x1"}.}

\item{logax, plot.legend, man.legend, ext, rel.axes, xlim, ylim, xlabel, stamp, verbose, CI}{Passed through to \code{\link[=kobe_all_in_one_gg]{kobe_all_in_one_gg()}}. See its documentation for details.}
}
\value{
A \strong{patchwork} object representing the scenario grid (also a \code{ggplot}).
}
\description{
Constructs a grid of Kobe plots for one scenario from \code{all_models}, using
\code{\link[=kobe_all_in_one_gg]{kobe_all_in_one_gg()}} for each model/data-source combination and arranging
the panels with \strong{patchwork}.
}
\details{
Layout \code{"3x2"} creates six panels: rows = models (Pella, Schaefer, Fox),
columns = data types (SDM, GLM). Layout \code{"3x1"} creates three panels, one
per model (preferring SDM if both SDM and GLM exist).
}
\examples{
\dontrun{
# One scenario, 3x2 grid:
build_scenario_KOBE_grid(all_models, "S1", layout = "3x2")

# One scenario, 3x1 grid (prefers SDM when both exist):
build_scenario_KOBE_grid(all_models, "S1", layout = "3x1")
}

}
\seealso{
\code{\link[=build_all_scenario_KOBE_grids]{build_all_scenario_KOBE_grids()}}, \code{\link[=kobe_all_in_one_gg]{kobe_all_in_one_gg()}}
}
