% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elu_nice_priors10.R
\name{elu_nice_priors10}
\alias{elu_nice_priors10}
\title{Save Prior vs Posterior Plots for Multiple Models, Plus Scenario Grids}
\usage{
elu_nice_priors10(
  models,
  priors_fun = priors.elu6,
  outdir = "FIG",
  width = 8,
  height = 4,
  dpi = 300
)
}
\arguments{
\item{models}{Named list of fitted model objects. Names must end in "P", "F", or "S",
prefixed by scenario tag (e.g. "S1P", "S1F", "S1S").}

\item{priors_fun}{Function to plot priors vs posterior for one model.
Signature: \code{priors_fun(model_obj, model_id, ...)} returning a ggplot.
Default: \code{priors.elu6}.}

\item{outdir}{Output directory for PNGs. Default: \code{"FIG"}.}

\item{width}{Width (in inches) of each individual plot. Default: 8.}

\item{height}{Height (in inches) of each individual plot. Default: 4.}

\item{dpi}{Resolution (dpi) for saved PNGs. Default: 300.}
}
\value{
Invisibly \code{NULL}. Called for side effects.
}
\description{
For each model in \code{models}, saves an individual prior–posterior plot.
Then, for each scenario (e.g. "S1"), collects its three submodels
("S1P", "S1F", "S1S"), stacks them as rows, and saves a combined grid.
}
\details{
\itemize{
\item Individual plots are named: \verb{priors_<model_name>.png}
\item Scenario grids are named: \verb{priors_<scenario>_grid.png},
where \verb{<scenario>} is the prefix (e.g. "S1").
\item Uses patchwork to stack the three ggplots in row order: Pella, Fox, Schaefer.
}
}
