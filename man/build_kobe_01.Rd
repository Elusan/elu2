% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_kobe_grids_scenario.R
\name{build_kobe_01}
\alias{build_kobe_01}
\title{Build a Kobe-plot grid for one scenario}
\usage{
build_kobe_01(
  all_models,
  scenario_name,
  layout = c("3x2", "3x1"),
  logax = FALSE,
  plot.legend = TRUE,
  man.legend = TRUE,
  ext = TRUE,
  rel.axes = FALSE,
  xlim = NULL,
  ylim = NULL,
  xlabel = NULL,
  stamp = NULL,
  verbose = TRUE,
  CI = 0.95
)
}
\arguments{
\item{all_models}{Named list of scenarios. Each element is a named list of
fitted models (objects accepted by \code{\link[=kobe_all_in_one_gg]{kobe_all_in_one_gg()}}) whose
names end with \code{".SDM"} or \code{".GLM"} and also contain the model letter
just before the dot (e.g., \code{"S1P.SDM"}, \code{"S1S.GLM"}, \code{"S1F.SDM"}).}

\item{scenario_name}{Character; name of the scenario to render (must exist
in \code{names(all_models)}).}

\item{layout}{Either \code{"3x2"} (default) or \code{"3x1"}.}

\item{logax, plot.legend, man.legend, ext, rel.axes, xlim, ylim, xlabel, stamp, verbose, CI}{Passed through to \code{\link[=kobe_safe]{kobe_safe()}} / \code{\link[=kobe_all_in_one_gg]{kobe_all_in_one_gg()}}.}
}
\value{
A \code{patchwork} object (a composed grid of six or three Kobe plots).
}
\description{
Create a \code{patchwork} grid of Kobe plots for a single scenario held inside
\code{all_models[[scenario_name]]}. Two layouts are supported:
\itemize{
\item \code{"3x2"} (default): rows = models P / S / F; columns = SDM | GLM.
\item \code{"3x1"}: one column; prefers SDM if present, otherwise GLM for each model.
}

Each panel is produced via \code{\link[=kobe_safe]{kobe_safe()}}, with a bold per-panel ID
placed at the top-left using \code{\link[=add_id_label]{add_id_label()}}.
}
\examples{
\dontrun{
g <- build_kobe_01(all_models, "S1", layout = "3x2", CI = 0.95)
g
}

}
