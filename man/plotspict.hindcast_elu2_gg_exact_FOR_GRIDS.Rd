% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotspict.hindcast_elu2_gg_exact_FOR_GRIDS.R
\name{plotspict.hindcast_elu2_gg_exact_FOR_GRIDS}
\alias{plotspict.hindcast_elu2_gg_exact_FOR_GRIDS}
\title{SPiCT hindcast plot (elu2 exact version; grid-friendly)}
\usage{
plotspict.hindcast_elu2_gg_exact_FOR_GRIDS(
  rep,
  add.mase = TRUE,
  CI = 0.95,
  verbose = TRUE,
  xlim = NULL,
  ylim = NULL,
  xlab = "Year",
  ylab = NA,
  plot.log = TRUE,
  legend.title = NULL,
  legend.pos = "topright",
  legend.ncol = 1,
  asp = 2,
  stamp = get.version()
)
}
\arguments{
\item{rep}{A fitted SPiCT result object \strong{with} \verb{$hindcast} attached and
consumable by \code{extract.hindcast.info()}.}

\item{add.mase}{Logical; if \code{TRUE}, annotate panels with MASE values. Default \code{TRUE}.}

\item{CI}{Confidence level for ribbons (e.g., \code{0.95}). Default \code{0.95}.}

\item{verbose}{Logical; handed to the extractor. Default \code{TRUE}.}

\item{xlim, ylim}{Optional numeric length-2 vectors for axis limits.}

\item{xlab, ylab}{Axis labels (kept for signature compatibility). The plot
sets \code{"Year"} and \code{"log(Index)"} for SPiCT parity when \code{ylab} is \code{NA}.}

\item{plot.log}{Logical; if \code{FALSE}, exponentiate \code{obs/pred/lc/uc}. Default \code{TRUE}.}

\item{legend.title}{Optional legend title; default \code{NULL} (no title).}

\item{legend.pos, legend.ncol, asp}{Ignored (kept for compatibility).}

\item{stamp}{Character stamp (e.g., \code{get.version()}), currently not drawn
(reserved for compatibility).}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object (faceted if multiple indices).
}
\description{
Produce SPiCT-style hindcast plots with ggplot2 for one or more index series.
Draws the reference fit (ribbon + line), colored peel prediction lines for
each hindcast peel, and a short \strong{dashed} grey tail segment for the final
prediction step of each peel. Observations are shown as large circles
(white pre-hindcast; colored during hindcast). Legend is forced to \strong{two
columns}: column 1 lists \emph{Ref} and peel labels; column 2 shows only \emph{obs}
and \emph{pred} entries.
}
\details{
\itemize{
\item Final segment of each peel is \code{linetype = "dashed"}, \code{color = "grey30"}.
\item Legend uses \strong{two columns}: \emph{Ref} + peels (col 1), \emph{obs}, \emph{pred} (col 2).
\item Facet strip background/text removed via \code{\link[=theme_minimal_compact2_hind]{theme_minimal_compact2_hind()}}.
\item The \emph{two stray circles at (0,0)} problem is avoided by using \code{NA} dummies
with \code{na.rm = TRUE} to create legend keys without drawing points.
}

Requires a helper \code{extract.hindcast.info()} available in the search path
(e.g., from \strong{spict} or your own wrapper in \strong{elu2}).
}
\seealso{
\code{\link[=theme_minimal_compact2_hind]{theme_minimal_compact2_hind()}}, \code{\link[=plot_hindcast_grid_scenario]{plot_hindcast_grid_scenario()}}
}
