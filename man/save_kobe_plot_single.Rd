% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_kobe_plot_single.R
\name{save_kobe_plot_single}
\alias{save_kobe_plot_single}
\title{Save a single Kobe plot to disk (robust)}
\usage{
save_kobe_plot_single(
  rep_obj,
  id_text,
  file_path,
  width = 6,
  height = 5,
  dpi = 300,
  ...
)
}
\arguments{
\item{rep_obj}{A fitted SPiCT-like object accepted by \code{kobe_all_in_one_gg()}.}

\item{id_text}{Character label (e.g., "S1P.SDM") drawn at top-left inside the panel.}

\item{file_path}{Full output PNG path (directories should exist; use \code{dir.create(recursive=TRUE)}).}

\item{width, height}{Numeric, \strong{inches} for PNG device. Default \verb{6 x 5}.}

\item{dpi}{Numeric PNG resolution. Default \code{300}.}

\item{...}{Passed to \code{\link[=kobe_safe]{kobe_safe()}} (e.g., \code{rel.axes}, \code{logax}, \code{CI}, etc.).}
}
\value{
Invisibly returns \code{TRUE} on success, \code{FALSE} (with a warning) on failure.
}
\description{
Builds a Kobe plot using \code{\link[=kobe_safe]{kobe_safe()}} (which wraps
\code{\link[=kobe_all_in_one_gg]{kobe_all_in_one_gg()}}) and saves it as a PNG.
The plot is annotated with a bold top-left ID via \code{\link[=add_id_label]{add_id_label()}}.
}
