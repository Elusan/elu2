% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_models_by_WAIC.R
\name{select_models_by_WAIC}
\alias{select_models_by_WAIC}
\title{Select models by WAIC within each (scenario × type)}
\usage{
select_models_by_WAIC(all_models)
}
\arguments{
\item{all_models}{Nested list like \code{all_models$S1$S1P.SDM}, \code{all_models$S1$S1F.GLM}, ...}
}
\value{
A list with:
\describe{
\item{\code{full}}{Long data.frame of all retained fits with columns:
\code{scenario}, \code{set_name}, \code{type}, \code{family}, \code{waic}, \code{convergence},
\code{nC}, \code{nI1}, \code{nI2}, \code{delta_st}, \code{weight_st}.}
\item{\code{best}}{One row per (scenario × type) — the smallest WAIC.}
\item{\code{wide}}{One row per (scenario × type); columns \verb{WAIC_*}, \verb{dWAIC_*}, \verb{w_*}
for families Pella, Schaefer, Fox.}
\item{\code{notes}}{Character vector of diagnostic messages (possibly length 0).}
}
}
\description{
Computes WAIC-based model selection for your nested structure
\verb{all_models$Sx$Sx\{P|S|F\}.\{SDM|GLM\}}. It:
\itemize{
\item extracts \code{WAIC_plug_total} robustly (from \code{model$report$WAIC_plug_total}
or \code{model$report()$WAIC_plug_total});
\item keeps only converged fits (\code{opt$convergence == 0}) with finite WAIC;
\item computes ΔWAIC and Akaike-like weights \strong{within each (scenario × type)};
\item identifies the best model per (scenario × type);
\item returns a wide table with WAIC/ΔWAIC/weights per family (Pella, Schaefer, Fox);
\item emits notes when data counts differ across models (compare WAIC with caution).
}
}
\examples{
\dontrun{
  sel <- select_models_by_WAIC(all_models)
  sel$best            # winner per (scenario × type)
  sel$wide            # compact comparison table
  sel$full            # full long table
  sel$notes           # any comparability warnings
}

}
