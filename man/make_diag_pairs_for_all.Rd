% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_diag_pairs_for_all.R
\name{make_diag_pairs_for_all}
\alias{make_diag_pairs_for_all}
\title{Batch: build SDM–GLM pairs for every scenario/model in a nested list}
\usage{
make_diag_pairs_for_all(
  all_models,
  keys = NULL,
  save = TRUE,
  width = 16,
  height = 10,
  dpi = 400,
  verbose = TRUE
)
}
\arguments{
\item{all_models}{Nested named list exactly as above.}

\item{keys}{Optional subset of base keys (e.g., c("S1P","S1S","S1F")) per scenario.
If NULL, inferred from names present.}

\item{save, width, height, dpi}{Passed to ggsave through \code{diag_pair_side_by_side()}.}

\item{verbose}{Print where each file is written.}
}
\value{
A \code{data.frame} with columns: \code{scenario}, \code{key}, \code{path}, \code{exists}.
}
\description{
Expects a nested \code{all_models} like you provided:
\preformatted{
all_models <- list(
  S1 = list(S1P.GLM=..., S1S.GLM=..., S1F.GLM=..., S1P.SDM=..., S1S.SDM=..., S1F.SDM=...),
  S2 = list(...), S3 = list(...), S4 = list(...)
)
}
For each scenario and each base key (e.g., S1P, S1S, S1F), it:
\enumerate{
\item ensures process residuals exist,
\item builds the side-by-side SDM/GLM diagnostic,
\item saves to \code{FIG/Diag_SDM_GLM/<Scenario>/Diagnostics_<Scenario>_<Key>_pair.png}.
}
}
