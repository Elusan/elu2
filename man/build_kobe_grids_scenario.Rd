% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_kobe_grids_scenario.R
\name{build_kobe_grids_scenario}
\alias{build_kobe_grids_scenario}
\title{Build Kobe-plot grids for \emph{all} scenarios (optionally save)}
\usage{
build_kobe_grids_scenario(
  all_models,
  layout = "3x2",
  save = TRUE,
  out_dir = file.path("FIG", "KobePhases"),
  width = if (layout == "3x2") 15 else 6,
  height = if (layout == "3x2") 6.5 else 10,
  dpi = 300,
  ...
)
}
\arguments{
\item{all_models}{Named list of scenarios. Each scenario is a list of fitted
models (see \code{\link[=build_kobe_01]{build_kobe_01()}} for naming expectations).}

\item{layout}{\code{"3x2"} (default) or \code{"3x1"}. Passed to \code{\link[=build_kobe_01]{build_kobe_01()}}.}

\item{save}{Logical; if \code{TRUE}, save each grid as a PNG to \code{out_dir}. Default \code{TRUE}.}

\item{out_dir}{Output directory for PNG files (created if needed).
Default \code{file.path("FIG", "KobePhases")}.}

\item{width, height}{Numeric; \strong{inches} for saved PNGs. Defaults depend on
\code{layout}: for \code{"3x2"} use \verb{15 x 6.5}, otherwise \verb{6 x 10}.}

\item{dpi}{Numeric; dots per inch for outputs. Default \code{300}.}

\item{...}{Additional arguments forwarded to \code{\link[=build_kobe_01]{build_kobe_01()}}
and ultimately to \code{\link[=kobe_safe]{kobe_safe()}} / \code{\link[=kobe_all_in_one_gg]{kobe_all_in_one_gg()}}.}
}
\value{
A named list of \code{patchwork} grids, one per scenario (invisible if
you only care about saved files).
}
\description{
Batch runner across every element in \code{all_models}. For each scenario,
calls \code{\link[=build_kobe_01]{build_kobe_01()}} to compose the grid, stores the result in a
named list, and (optionally) saves a PNG per scenario.
}
\examples{
\dontrun{
grids <- build_kobe_grids_scenario(all_models, layout = "3x2",
                                   save = TRUE, out_dir = "FIG/KobePhases")
grids$S1  # view Scenario 1 grid
}

}
