% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_params_SDM_GLM555.R
\name{extract_params_SDM_GLM555}
\alias{extract_params_SDM_GLM555}
\title{Extract K, r, MSY, B/Bmsy, F/Fmsy (+ CIs) from SDM+GLM SPiCT models}
\usage{
extract_params_SDM_GLM555(model_list, scenarios = NULL, verbose = TRUE)
}
\arguments{
\item{model_list}{A \strong{named} list of SPiCT fits named like
\code{"<scenario><family>.<type>"} (e.g., \code{"S3F.SDM"}).}

\item{scenarios}{Optional character vector of scenarios to include
(e.g., \code{c("S1","S2")}). If \code{NULL}, scenarios are inferred from names.}

\item{verbose}{Logical; print progress by scenario? Default \code{TRUE}.}
}
\value{
A tidy \code{data.frame} with columns:
\itemize{
\item \code{scenario} — e.g., \code{"S1"},
\item \code{type} — \code{"SDM"} or \code{"GLM"},
\item \code{family} — \code{"Fox"}, \code{"Schaefer"}, or \code{"Pella"},
\item \code{param} — one of \code{"K"}, \code{"r"}, \code{"MSY"}, \code{"B/Bmsy"}, \code{"F/Fmsy"},
\item \code{year} — embedded year for ratio labels if present, else \code{NA_integer_},
\item \code{est}, \code{lo}, \code{hi} — estimate and bounds.
}

Factors are set to:
\code{family = c("Fox","Schaefer","Pella")},
\code{type   = c("SDM","GLM")},
\code{param  = c("K","r","MSY","B/Bmsy","F/Fmsy")}.
}
\description{
Builds a tidy table of selected parameters and relative status metrics for
\strong{both} SDM and GLM model types across scenarios and Fox / Schaefer / Pella
families. This wrapper relies on your helper \code{make_spict_summary_df1_nopipe()}
to produce wide per-scenario summaries, then reshapes and filters them.
}
\details{
Parameter labels in the wide table are parsed and mapped to the keys
\code{"K"}, \code{"r"}, \code{"MSY"}, \code{"B/Bmsy"}, \code{"F/Fmsy"}. If a ratio label embeds a year
(e.g., \verb{$B_\{2022\}/B_\{MSY\}$}), that year is extracted into the \code{year} column;
otherwise \code{year} is \code{NA_integer_}. Non-target parameters are discarded.

Scenarios are auto-detected from \code{names(model_list)} when \code{scenarios} is \code{NULL}.
Missing triplet members (e.g., no \code{"S1F.GLM"}) are skipped silently.
}
\section{Required input layout}{

The function expects a \strong{named} list \code{model_list} whose names follow
\code{"<scenario><family>.<type>"}, for example:
\code{"S1F.SDM"}, \code{"S1S.GLM"}, \code{"S1P.SDM"}, … where
\itemize{
\item \verb{scenario = S\\\\d+} (e.g., \code{"S1"}, \code{"S2"}, …),
\item \verb{family ∈ \{F,S,P\}} → Fox, Schaefer, Pella,
\item \verb{type ∈ \{SDM, GLM\}}.
}

Your helper \code{make_spict_summary_df1_nopipe(models_triplet, model_names)} should
return a \strong{wide} data frame with columns like:
\code{Scenario}, \code{Parameters},
\code{Fox_estimate}, \code{Fox_low}, \code{Fox_up},
\code{Schaefer_estimate}, \code{Schaefer_low}, \code{Schaefer_up},
\code{Pella_estimate}, \code{Pella_low}, \code{Pella_up}.
}

\examples{
\dontrun{
# Example structure:
fits <- list(
  S1F.SDM = fit_fox_sdm,     S1S.SDM = fit_schaefer_sdm, S1P.SDM = fit_pella_sdm,
  S1F.GLM = fit_fox_glm,     S1S.GLM = fit_schaefer_glm, S1P.GLM = fit_pella_glm,
  S2F.SDM = fit2_fox_sdm     # etc…
)

# Ensure your helper is available:
# source("make_spict_summary_df1_nopipe.R")

df <- extract_params_SDM_GLM(fits, scenarios = c("S1","S2"))
subset(df, param \%in\% c("B/Bmsy","F/Fmsy"))
}

}
\seealso{
\code{\link[=compute_param_pct_change]{compute_param_pct_change}} for
computing \% changes vs. a base scenario from this tidy output.
}
