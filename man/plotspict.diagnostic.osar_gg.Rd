% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotspict.diagnostic.osar_gg.R
\name{plotspict.diagnostic.osar_gg}
\alias{plotspict.diagnostic.osar_gg}
\title{OSAR diagnostics (ggplot2) faithfully matching SPiCT base panels}
\usage{
plotspict.diagnostic.osar_gg(
  rep,
  lag.max = 4,
  qlegend = TRUE,
  plot.data = TRUE,
  mfcol = FALSE,
  stamp = get.version()
)
}
\arguments{
\item{rep}{A fitted SPiCT result list (with \verb{$inp}, \verb{$osar}, \verb{$diagn}).}

\item{lag.max}{Maximum lag to display in ACF panels. Default \code{4}.}

\item{qlegend}{Kept for API parity; legends are not drawn. Default \code{TRUE}.}

\item{plot.data}{If \code{TRUE}, include the top row with log-data by series.
Default \code{TRUE}.}

\item{mfcol}{Kept for API parity; not used (layout via \pkg{patchwork}).
Default \code{FALSE}.}

\item{stamp}{Caption text for the full figure (e.g., a version string).
Default \code{get.version()}.}
}
\value{
A \pkg{patchwork} / \pkg{ggplot2} object combining all panels.
}
\description{
Builds a multi-row, multi-column grid of diagnostic panels (optionally
including raw data at the top) for \strong{Catch}, optional \strong{Effort}, and each
\strong{Index} series found in a SPiCT \code{rep} object. Rows show, in order:
data (optional), OSA residuals, residual ACF, and QQ ("Sample quantiles")
panels. Axes, p-value coloring, and QQ scaling are aligned with SPiCT’s base
plotting style.
}
\details{
\strong{Notes}
\itemize{
\item Expects \code{rep$osar} and \code{rep$diagn} produced by \code{calc.osa.resid(rep)} and
related SPiCT routines.
\item X-axis year breaks are integer (annual) if the span is short; otherwise
they fall every 5 years for readability.
\item QQ panels emulate base \code{qqnorm/qqline} with ~4\% padding
(as in \code{par(xaxs = "r", yaxs = "r")}).
}
}
\examples{
\dontrun{
  # Assuming `rep` is a SPiCT report list with OSA residuals:
  p <- plotspict.diagnostic.osar_gg(rep)
  p
}

}
