% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elu_nice_priors_999.R
\name{elu_nice_priors_999}
\alias{elu_nice_priors_999}
\title{Save Prior vs Posterior Plots for Multiple SPiCT Models}
\usage{
elu_nice_priors_999(
  models,
  priors_fun = priors.elu6,
  outdir = "FIG",
  width = 8,
  height = 4,
  dpi = 300
)
}
\arguments{
\item{models}{A named list of fitted SPiCT model objects (e.g., results from \code{fit.elu2()} or similar).}

\item{priors_fun}{A function used to generate the prior vs posterior plot for a single model.
Default is \code{\link{priors.elu6}}.}

\item{outdir}{Output directory to save the PNG figures. If it does not exist, it will be created.
Default: \code{"FIG"}.}

\item{width}{Width of each PNG figure in inches. Default: \code{8}.}

\item{height}{Height of each PNG figure in inches. Default: \code{4}.}

\item{dpi}{Resolution of the output PNG figure in dots per inch. Default: \code{300}.}
}
\value{
Invisibly returns \code{NULL}. Side effect: saves PNG files to disk.
}
\description{
Generates and saves prior vs posterior distribution plots (as PNG files) for each fitted model object in a list.
}
\details{
For each model in the input list, the function generates a prior–posterior comparison plot using
the specified \code{priors_fun} and saves it as a PNG file named \code{priors_<model>.png}
in the specified output directory.
}
\examples{
\dontrun{
models <- list(S1P = fit1, S1F = fit2, S1S = fit3)
elu_nice_priors_999(models)
}

}
