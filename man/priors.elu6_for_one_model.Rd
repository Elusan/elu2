% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors-one-model.R
\name{priors.elu6_for_one_model}
\alias{priors.elu6_for_one_model}
\title{Prior vs posterior density (single model; single or multi-panel)}
\usage{
priors.elu6_for_one_model(
  rep,
  model_id = NULL,
  do.plot = NULL,
  stamp = get.version(),
  CI = 0.95
)
}
\arguments{
\item{rep}{A fitted SPiCT result (\code{spictcls}).}

\item{model_id}{Optional character used in labeling (not required).}

\item{do.plot}{Optional integer cap on number of priors to draw; if \code{1},
returns a single \code{ggplot} panel immediately.}

\item{stamp}{Optional message emitted when returning a patchwork (unchanged).}

\item{CI}{Confidence level for parameter extraction (\code{0.95} by default).}
}
\value{
Either a \code{ggplot} (single panel) or a \code{patchwork} object.
}
\description{
Builds prior–posterior density panels for all \strong{active} priors in a fitted
SPiCT model. If \code{do.plot = 1}, returns the \strong{first} active prior panel
directly (useful for embedding a single density). Otherwise returns a
patchwork of panels. Gamma priors are handled; x-scale is log when dynamic
range is wide.
}
\examples{
\dontrun{
# First prior only:
priors.elu6_for_one_model(all_models$S1$S1P.SDM, do.plot = 1)
# All active priors:
priors.elu6_for_one_model(all_models$S1$S1P.SDM)
}
}
\concept{ELU2-priors}
