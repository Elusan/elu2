% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elu_prior_posterior_grid.R
\name{elu_prior_posterior_grid}
\alias{elu_prior_posterior_grid}
\title{Grid of Prior and Posterior Distributions Across Multiple Models}
\usage{
elu_prior_posterior_grid(
  models,
  priors_fun = priors.elu6,
  width = 15,
  height = 10,
  dpi = 300,
  outdir = "FIG",
  file_prefix = NULL,
  return_patchwork = TRUE
)
}
\arguments{
\item{models}{A named list of fitted model objects (e.g., from \code{fit.elu2()}), where each element represents a scenario or model variant.}

\item{priors_fun}{Function to generate prior/posterior plots for a single model. Should take arguments (\code{rep}, \code{model_id}, etc.) and return a list of ggplot objects, one for each prior (default: \code{priors.elu6}).}

\item{width}{Numeric; width (in inches) of the saved plot grid image.}

\item{height}{Numeric; height (in inches) of the saved plot grid image.}

\item{dpi}{Numeric; dots per inch for saved image (default: 300).}

\item{outdir}{Directory to save the image (default: \code{"FIG"}). If NULL, no file is written.}

\item{file_prefix}{Character prefix for the saved file name. Default is to generate one from the first model name.}

\item{return_patchwork}{Logical; if TRUE, returns the combined patchwork plot object. If FALSE, returns nothing (useful for batch saving).}
}
\value{
A patchwork plot object representing the grid of prior/posterior plots, or invisibly NULL if \code{return_patchwork = FALSE}.
}
\description{
Generates a grid of plots comparing prior and posterior distributions for all active priors across multiple fitted ELU models. Each row in the grid corresponds to a model and each column to a prior parameter, enabling easy comparison of prior/posterior overlap and informativeness by model and parameter.
}
\details{
The function collects all unique active priors across the provided models. For each model (row) and prior (column), it generates a prior/posterior plot using \code{priors_fun}. If a prior is not available for a model, the corresponding cell is left blank. The resulting grid is saved as a PNG if \code{outdir} is not NULL and is also returned as a patchwork object if \code{return_patchwork = TRUE}.
}
\examples{
\dontrun{
grid_plot <- elu_prior_posterior_grid(list(Fox = fit1, Schaefer = fit2, Pella = fit3))
}
}
