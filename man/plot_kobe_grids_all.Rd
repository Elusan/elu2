% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_kobe_grids_all.R
\name{plot_kobe_grids_all}
\alias{plot_kobe_grids_all}
\title{Plot/save Kobe grids for many scenarios in one go}
\usage{
plot_kobe_grids_all(
  all_models,
  scenario_names = names(all_models),
  save = TRUE,
  out_dir = file.path("FIG", "KobePhases"),
  width = 12,
  height = 5.5,
  dpi = 300,
  man.legend = FALSE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{all_models}{Named list of scenarios. Each scenario is a named list of
fitted model objects accepted by \code{\link[=kobe_all_in_one_gg]{kobe_all_in_one_gg()}}.}

\item{scenario_names}{Character vector of scenario names to process.
Default: \code{names(all_models)} (all scenarios).}

\item{save}{Logical; save each grid PNG to \code{out_dir}. Default \code{TRUE}.}

\item{out_dir}{Directory where PNGs are written when \code{save = TRUE}.
Default \code{file.path("FIG", "KobePhases")}.}

\item{width, height}{Numeric; inches for each grid PNG. Default \verb{12 x 5.5}.}

\item{dpi}{Numeric; dots per inch for PNGs. Default \code{300}.}

\item{man.legend}{Logical; show management-scenario legends inside panels.
Default \code{FALSE} to avoid an extra legend row changing the layout.}

\item{verbose}{Logical; emit progress messages. Default \code{TRUE}.}

\item{...}{Additional arguments forwarded to \code{\link[=plot_kobe_grid_scenario]{plot_kobe_grid_scenario()}}
and ultimately to \code{\link[=kobe_safe]{kobe_safe()}} (e.g., \code{rel.axes}, \code{CI}, \code{logax}).}
}
\value{
Invisibly returns a list with:
\itemize{
\item \code{grids} — named list of patchwork objects (one per scenario)
\item \code{paths} — named character vector of file paths (only when \code{save = TRUE})
}
}
\description{
Iterates over scenarios and calls \code{\link[=plot_kobe_grid_scenario]{plot_kobe_grid_scenario()}} for each.
By default it processes \strong{all} scenarios found in \code{all_models}, and will save
one PNG per scenario to \code{out_dir}. Returns the patchwork objects (one per
scenario) invisibly; if \code{save = TRUE}, also returns the file paths.
}
\examples{
\dontrun{
# Build & save grids for all scenarios
plot_kobe_grids_all(all_models,
                    save = TRUE, out_dir = "FIG/KobePhases",
                    width = 12, height = 5.5, dpi = 300,
                    man.legend = FALSE,
                    rel.axes = FALSE, CI = 0.95)

# Only build (no files written), and for a subset:
res <- plot_kobe_grids_all(all_models, scenario_names = c("S1","S3"),
                           save = FALSE, rel.axes = FALSE, CI = 0.95)
print(res$grids$S1)
}
}
