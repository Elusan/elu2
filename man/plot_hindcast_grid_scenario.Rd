% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_hindcast_grid_scenario.R
\name{plot_hindcast_grid_scenario}
\alias{plot_hindcast_grid_scenario}
\title{Hindcast grid (2×3) for one scenario}
\usage{
plot_hindcast_grid_scenario(
  all_models,
  scenario,
  npeels = 2,
  peel.dtc = FALSE,
  mc.cores = 1,
  CI = 0.95,
  verbose = FALSE,
  add.mase = TRUE,
  show_legends = TRUE,
  save = TRUE,
  out_dir = file.path("FIG", "Hindcast"),
  filename = NULL,
  width = 12,
  height = 5,
  dpi = 300
)
}
\arguments{
\item{all_models}{Named list-of-lists by scenario, e.g. \code{all_models$S1$S1P.SDM}.}

\item{scenario}{Name of the scenario to plot, e.g. \code{"S1"}.}

\item{npeels, peel.dtc, mc.cores}{Hindcast controls.}

\item{CI, verbose, add.mase}{Passed to \code{\link[=plotspict.hindcast_elu2_gg_exact_FOR_GRIDS]{plotspict.hindcast_elu2_gg_exact_FOR_GRIDS()}}.}

\item{show_legends}{Logical; keep per-panel legends (default \code{TRUE}). Set \code{FALSE} to declutter.}

\item{save}{Logical; save PNG to \code{out_dir}. Default \code{TRUE}.}

\item{out_dir}{Output directory (created if missing). Default \code{file.path("FIG", "Hindcast")}.}

\item{filename}{Optional filename; default \code{"HindcastGrid_<scenario>_2x3.png"}.}

\item{width, height, dpi}{PNG device settings (inches, inches, dpi).}
}
\value{
Invisibly, a list with \code{patch} (patchwork object) and \code{file} (path or \code{NA}).
}
\description{
Builds a 2×3 grid: columns = \strong{Pella}, \strong{Schaefer}, \strong{Fox};
rows = \strong{SDM} (top), \strong{GLM} (bottom). Panel labels are like \code{"S1P.SDM"}.
Missing models or plotting errors produce placeholders.
}
