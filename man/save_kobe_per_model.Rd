% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_kobe_per_model.R
\name{save_kobe_per_model}
\alias{save_kobe_per_model}
\title{Save individual Kobe plots for one or more scenarios}
\usage{
save_kobe_per_model(
  all_models,
  scenario_names = names(all_models),
  out_dir = file.path("FIG", "per_model"),
  width = 6,
  height = 5,
  dpi = 300,
  add_id = TRUE,
  ...
)
}
\arguments{
\item{all_models}{Named list of scenarios. Each scenario is a named list of
fitted model objects accepted by \code{\link[=kobe_all_in_one_gg]{kobe_all_in_one_gg()}}.}

\item{scenario_names}{Character vector of scenario names to process.
Default: \code{names(all_models)} (process all).}

\item{out_dir}{Base directory for per-model PNGs (subdirs per scenario
are created automatically). Default \code{file.path("FIG","per_model")}.}

\item{width, height}{Numeric; inches for saved PNGs. Default \verb{6 x 5}.}

\item{dpi}{Numeric; dots per inch for PNGs. Default \code{300}.}

\item{add_id}{Logical; add top-left ID label using \code{\link[=add_id_label]{add_id_label()}}.
Default \code{TRUE}.}

\item{...}{Additional arguments forwarded to \code{\link[=kobe_safe]{kobe_safe()}}
(e.g., \code{rel.axes}, \code{CI}, \code{logax}, etc.).}
}
\value{
Invisibly returns a named list: for each scenario, a named character
vector of file paths keyed by model name.
}
\description{
Iterates through the fitted models in each scenario of \code{all_models},
builds a Kobe plot via \code{\link[=kobe_safe]{kobe_safe()}}, and saves one PNG per model
under \verb{FIG/per_model/<scenario>/}. Returns the file paths invisibly.
}
\examples{
\dontrun{
save_kobe_per_model(all_models, scenario_names = c("S1","S2"),
                    rel.axes = FALSE, CI = 0.95)
}
}
