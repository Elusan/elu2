% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_kobe_grid_scenario.R
\name{plot_kobe_grid_scenario}
\alias{plot_kobe_grid_scenario}
\title{Plot a 2×3 Kobe grid for one scenario (and optionally save)}
\usage{
plot_kobe_grid_scenario(
  all_models,
  scenario_name,
  save = FALSE,
  out_dir = file.path("FIG", "KobePhases"),
  width = 12,
  height = 5.5,
  dpi = 300,
  man.legend = FALSE,
  ...
)
}
\arguments{
\item{all_models}{Named list of scenarios. Each scenario is a named list of
fitted model objects with names like \code{"S1P.SDM"}, \code{"S1S.GLM"}, etc.}

\item{scenario_name}{Scenario to render (must exist in \code{all_models}).}

\item{save}{Logical; save a PNG of the grid. Default \code{FALSE}.}

\item{out_dir}{Directory to save the PNG if \code{save=TRUE}. Default
\code{file.path("FIG","KobePhases")}.}

\item{width, height}{Numeric; inches for the PNG. Default \verb{12 x 5.5}.}

\item{dpi}{Numeric; dots per inch. Default \code{300}.}

\item{man.legend}{Logical; show management-scenario legends in panels.
Default \code{FALSE} (helps avoid a legend-row changing the layout).}

\item{...}{Additional arguments forwarded to \code{\link[=kobe_safe]{kobe_safe()}}
(e.g., \code{rel.axes}, \code{CI}, \code{logax}, etc.).}
}
\value{
A patchwork object (the composed 2×3 grid). Invisibly returns the
object even if \code{save=TRUE}.
}
\description{
Builds a patchwork grid for a single scenario: top row shows SDM models
(P, S, F) and bottom row shows GLM models (P, S, F). Each panel is produced
via \code{\link[=kobe_safe]{kobe_safe()}} and tagged with its model ID using
\code{\link[=add_id_label]{add_id_label()}}. If \code{save=TRUE}, also saves a PNG to \code{out_dir}.
}
\examples{
\dontrun{
g <- plot_kobe_grid_scenario(all_models, "S1",
                             rel.axes = FALSE, CI = 0.95)
print(g)
}
}
