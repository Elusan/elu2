% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Tables_Grouped_WAIC_MAPE_56_exact.R
\name{Tables_Grouped_WAIC_MAPE_56_exact}
\alias{Tables_Grouped_WAIC_MAPE_56_exact}
\title{Export Combined SDM+GLM SPiCT Scenario Tables with Grouped Separation (+ WAIC & MAPE) — EXACT STYLE}
\usage{
Tables_Grouped_WAIC_MAPE_56_exact(
  model_list,
  output_dir = ".",
  scenarios = NULL,
  param_groups = NULL,
  verbose = TRUE,
  caption = NULL,
  include_waic = TRUE,
  include_mape = TRUE,
  waic_digits = 1,
  mape_digits = 1,
  cand_pred_names = NULL
)
}
\arguments{
\item{model_list}{Named flat list of SPiCT fits with names like \code{"S1F.SDM"} and \code{"S1F.GLM"}.
Names must follow the pattern \code{<scenario><family>.<type>} where \code{scenario="S\\d+"},
\code{family in \{"F","S","P"\}} for Fox, Schaefer, Pella, and \code{type in \{"SDM","GLM"\}}.}

\item{output_dir}{Directory where \code{.tex} files are written. Must exist.}

\item{scenarios}{Optional character vector of scenario IDs (e.g., \code{c("S1","S2")}). If \code{NULL},
scenarios are auto-detected from \code{names(model_list)}.}

\item{param_groups}{Optional list of three character vectors defining the row order and sectioning:
group 1 (process & observation SDs), group 2 (stochastic reference points),
group 3 (stock status). If \code{NULL}, sensible defaults are used.}

\item{verbose}{Logical; print progress messages while writing files. Default \code{TRUE}.}

\item{caption}{Optional caption string (kept for API symmetry; not inserted in the EXACT style output).}

\item{include_waic}{Logical; if \code{TRUE} append WAIC row. Default \code{TRUE}.}

\item{include_mape}{Logical; if \code{TRUE} append pooled MAPE row. Default \code{TRUE}.}

\item{waic_digits}{Integer; decimals for WAIC cells. Default \code{1}.}

\item{mape_digits}{Integer; decimals for MAPE cells. Default \code{1}.}

\item{cand_pred_names}{Optional character vector of candidate report element names to obtain
index predictions for pooled MAPE when \code{fit$osar$Ihat} is unavailable.}
}
\value{
Invisibly, a named list of character scalars containing the LaTeX for each scenario.
Files are written as \code{file.path(output_dir, paste0("table_scenario", scen, "_SDM_GLM.tex"))}.
}
\description{
Produces LaTeX tables that exactly match your specified style:
row banding with \code{\\rowcolors}, bold \emph{Fox/Schaefer/Pella-Tomlinson} headers with
custom \code{\\cmidrule(l\{3pt\}r\{3pt\})}, a rule after every row, white section banners,
and compact two-line cells (bold estimate on line 1; \code{\\scriptsize} CI on line 2).
Appends WAIC and pooled MAPE rows per scenario. Writes one \code{table_scenario<SX>_SDM_GLM.tex}
per scenario and invisibly returns the LaTeX as a named list.
}
\details{
This function writes raw LaTeX (no \pkg{kableExtra}) to guarantee byte-for-byte styling.
Ensure your LaTeX preamble includes \code{\\usepackage[table]{xcolor}} and \code{\\usepackage{booktabs}}.
}
\examples{
\dontrun{
out <- All_scenarios_Tables_Grouped_WAIC_MAPE_56_exact(model_list, output_dir = "TABLES")
}

}
