% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_kobe_individuals_one_go.R
\name{save_kobe_individuals_one_go}
\alias{save_kobe_individuals_one_go}
\title{Save all individual Kobe plots under FIG/KobeIndividuals}
\usage{
save_kobe_individuals_one_go(
  all_models,
  out_root = file.path("FIG", "KobeIndividuals"),
  per_scenario_subfolders = TRUE,
  width = 6,
  height = 5,
  dpi = 300,
  ...
)
}
\arguments{
\item{all_models}{Named list of scenarios. Each \code{all_models[[scenario]]} is
a named list of fitted objects. Names should contain the submodel ID you
want on the PNG (e.g., "S1P.SDM", "S1F.GLM").}

\item{out_root}{Root output folder (created if needed). Default \code{"FIG/KobeIndividuals"}.}

\item{per_scenario_subfolders}{Logical; if \code{TRUE} (default), save each scenario
under \verb{FIG/KobeIndividuals/<Scenario>/}. If \code{FALSE}, save all PNGs flat in \code{out_root}.}

\item{width, height}{PNG size in \strong{inches}. Default \verb{6 x 5}.}

\item{dpi}{PNG resolution. Default \code{300}.}

\item{...}{Passed to \code{\link[=kobe_safe]{kobe_safe()}} (e.g., \code{rel.axes = FALSE}, \code{CI = 0.95}, etc.).}
}
\value{
A data.frame with columns: \code{scenario}, \code{submodel}, \code{file_path}, \code{ok} (logical).
}
\description{
For each scenario in \code{all_models} and for each submodel (e.g., "S1P.SDM",
"S1S.GLM", etc.), this function:
\enumerate{
\item Ensures the directory \verb{FIG/KobeIndividuals/<Scenario>/} exists
\item Builds the plot via \code{\link[=kobe_safe]{kobe_safe()}} + \code{\link[=add_id_label]{add_id_label()}}
\item Saves a PNG named \verb{<submodel>.png} inside that scenario subfolder.
}

Returns a data.frame log (scenario, submodel, file_path, ok).
}
\examples{
\dontrun{
log_df <- save_kobe_individuals(all_models,
                                out_root = file.path("FIG", "KobeIndividuals"),
                                rel.axes = FALSE, CI = 0.95)
head(log_df)
}

}
