% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/my_plot_manage_grid_2x3.R
\name{my_plot_manage_grid_2x3}
\alias{my_plot_manage_grid_2x3}
\title{2×3 management grid: Biomass, F, Catch, B/Bmsy, F/Fmsy, Kobe}
\usage{
my_plot_manage_grid_2x3(
  obj,
  save = FALSE,
  out_dir = file.path("FIG", "Manage"),
  file_stub = NULL,
  width = 18,
  height = 10,
  dpi = 300
)
}
\arguments{
\item{obj}{A fitted \code{spictcls} object (e.g., \code{m1}) or the same object after
\code{manage()} (e.g., \code{rman} with \verb{$man} present).}

\item{save}{Logical; if \code{TRUE}, save a PNG to \code{out_dir}. Default \code{FALSE}.}

\item{out_dir}{Directory where the PNG is written when \code{save = TRUE}.
Default \code{file.path("FIG","Manage")}.}

\item{file_stub}{Optional file name (without extension). If \code{NULL}, a sensible
name is generated from whether \code{obj} has \verb{$man}.}

\item{width, height, dpi}{Graphics device size (inches) and resolution for saving.
Defaults: \code{width = 18}, \code{height = 10}, \code{dpi = 300}.}
}
\value{
A patchwork object (the 2×3 grid). If \code{save = TRUE}, the PNG is
written and the patchwork object is returned invisibly.
}
\description{
Builds a compact 2×3 grid of management diagnostics for a \strong{SPiCT} object,
accepting either a fitted report (\code{spictcls}, e.g. \code{m1}) or the same object
after \code{manage()} (i.e., with \verb{$man}, e.g. \code{rman}).

\strong{Layout}
\itemize{
\item \strong{Row 1:} \code{my_plot_manage_biomass_panel()} (legend ON only if scenarios exist),
\code{my_plot_manage_f_panel()}, \code{my_plot_manage_catch_panel()}
\item \strong{Row 2:} \code{my_plot_manage_bbmsy_panel()}, \code{my_plot_manage_ffmsy_panel()},
\code{my_plot_kobe_all_management_scenario()} (legend OFF)
}

The wrapper preserves each panel’s native behaviour. The Biomass panel carries
the scenario legend when scenarios exist; all other panels hide their legends.
}
\details{
ELU2 management dashboard (2×3) for fitted or managed SPiCT objects
}
\seealso{
\code{\link{my_plot_manage_biomass_panel}},
\code{\link{my_plot_manage_f_panel}},
\code{\link{my_plot_manage_catch_panel}},
\code{\link{my_plot_manage_bbmsy_panel}},
\code{\link{my_plot_manage_ffmsy_panel}},
\code{\link{my_plot_kobe_all_management_scenario}}
}
