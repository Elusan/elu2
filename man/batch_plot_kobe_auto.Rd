% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch_plot_kobe_auto.R
\name{batch_plot_kobe_auto}
\alias{batch_plot_kobe_auto}
\title{Batch-export Kobe 2×3 grids for an arbitrary set of models}
\usage{
batch_plot_kobe_auto(
  models,
  out_dir = "FIG/KobePhasesNEW",
  file_basename = NULL,
  width = 20,
  height = 9,
  dpi = 400,
  man.legend = FALSE,
  save = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{models}{A list of fitted model objects. The list can be unnamed; if so,
names are generated as \code{"scenario_1"}, \code{"scenario_2"}, … by position.}

\item{out_dir}{Destination directory (see flexibility notes above). Default
\code{"FIG/KobePhasesNEW"}.}

\item{file_basename}{Basename for output files (see flexibility notes above).
Default \code{NULL} meaning \code{"<name>_custom_grid"}.}

\item{width, height}{Plot size in inches passed to \code{\link[=plot_kobe_grid_2x3_last]{plot_kobe_grid_2x3_last()}}.
Defaults \code{width = 20}, \code{height = 9}.}

\item{dpi}{Resolution passed to \code{\link[=plot_kobe_grid_2x3_last]{plot_kobe_grid_2x3_last()}}. Default \code{400}.}

\item{man.legend}{Logical, forwarded to \code{\link[=plot_kobe_grid_2x3_last]{plot_kobe_grid_2x3_last()}} to show/hide
the management legend inside the Kobe grid. Default \code{FALSE}.}

\item{save}{Logical, forwarded to \code{\link[=plot_kobe_grid_2x3_last]{plot_kobe_grid_2x3_last()}} indicating whether
to write image files to disk. Default \code{TRUE}.}

\item{verbose}{Logical; if \code{TRUE}, prints progress messages. Default \code{TRUE}.}

\item{...}{Additional arguments forwarded to \code{\link[=plot_kobe_grid_2x3_last]{plot_kobe_grid_2x3_last()}}.}
}
\value{
A \code{data.frame} with one row per model containing:
\itemize{
\item \code{model} — model name used,
\item \code{out_dir} — final output directory,
\item \code{file_basename} — final basename used,
\item \code{ok} — \code{TRUE} if the call succeeded, \code{FALSE} otherwise,
\item \code{error} — error message when \code{ok = FALSE}, otherwise \code{NA},
\item \code{path} — return value from \code{\link[=plot_kobe_grid_2x3_last]{plot_kobe_grid_2x3_last()}} when available.
}
}
\description{
A flexible batch runner around \code{\link[=plot_kobe_grid_2x3_last]{plot_kobe_grid_2x3_last()}} that \strong{does not
assume any fixed set of scenario names}. It iterates over the supplied
\code{models} list (any length, any names), creates output directories as needed,
and renders/saves each model’s 2×3 Kobe-style grid.

The arguments \code{out_dir} and \code{file_basename} are deliberately flexible so you
can control per-model destinations and filenames:
\itemize{
\item \strong{\code{out_dir}} can be:
\itemize{
\item a single string (all files saved in that folder),
\item a \strong{named} character vector keyed by model names,
\item a function \verb{function(name)} returning the directory for that model.
}
\item \strong{\code{file_basename}} can be:
\itemize{
\item \code{NULL} (defaults to \code{"<name>_custom_grid"}),
\item a \strong{named} character vector keyed by model names,
\item a function \verb{function(name)} returning the basename for that model,
\item a single un-named string (use the same basename for every model).
}
}
}
\details{
\itemize{
\item If \code{models} contains duplicated names, they are made unique via
\code{\link[base:make.unique]{base::make.unique()}} and a warning is issued.
\item Any directories required by \code{out_dir} are created (recursively) if missing.
}
}
\examples{
\dontrun{
# 1) All to a single folder, default basenames "<name>_custom_grid"
batch_plot_kobe_auto(
  models   = all_models,
  out_dir  = "FIG/KobePhasesNEW",
  width = 20, height = 9, dpi = 400,
  man.legend = FALSE
)

# 2) Per-model folders via a function:
batch_plot_kobe_auto(
  models   = all_models,
  out_dir  = function(nm) file.path("FIG", "KobePhasesNEW", nm)
)

# 3) Mixed folders via a named vector:
batch_plot_kobe_auto(
  models  = all_models,
  out_dir = c(S1 = "FIG/KobePhasesNew", S2 = "FIG/KobePhasesNEW")
)

# 4) Force a single basename for all:
batch_plot_kobe_auto(
  models        = all_models,
  file_basename = "mygrid"
)
}

}
\seealso{
\code{\link[=plot_kobe_grid_2x3_last]{plot_kobe_grid_2x3_last()}}
}
