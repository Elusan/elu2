% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_kobe_grids_scenario.R
\name{kobe_safe}
\alias{kobe_safe}
\title{Safely build a single Kobe plot (robust to failures)}
\usage{
kobe_safe(
  rep_obj,
  logax = FALSE,
  plot.legend = TRUE,
  man.legend = TRUE,
  ext = TRUE,
  rel.axes = FALSE,
  xlim = NULL,
  ylim = NULL,
  labpos = c(1, 1),
  xlabel = NULL,
  stamp = NULL,
  verbose = TRUE,
  CI = 0.95
)
}
\arguments{
\item{rep_obj}{A fitted model object compatible with
\code{\link[=kobe_all_in_one_gg]{kobe_all_in_one_gg()}} (e.g., \code{spictcls} or \code{elu2::fit.elu2} result).}

\item{logax}{Logical; log-scale both axes? Default \code{FALSE}.}

\item{plot.legend}{Logical; show legend for MSY / E(B\link{∞}) / True. Default \code{TRUE}.}

\item{man.legend}{Logical; show management-scenario legend (if present). Default \code{TRUE}.}

\item{ext}{Logical; add relative axes (B/Bmsy, F/Fmsy) on top/right when
\code{rel.axes = FALSE}. Default \code{TRUE}.}

\item{rel.axes}{Logical; plot axes as relative units (B/Bmsy vs F/Fmsy).
Default \code{FALSE} (absolute Ft vs biomass).}

\item{xlim, ylim}{Numeric length-2; plot limits. If \code{NULL}, computed internally.}

\item{labpos}{Integer length-2; kept for API parity (not used by ggplot). Default \code{c(1, 1)}.}

\item{xlabel}{Optional x-axis label override.}

\item{stamp}{Optional character version stamp. If \code{NULL}, no stamp.}

\item{verbose}{Logical; print diagnostics. Default \code{TRUE}.}

\item{CI}{Numeric in (0,1); confidence level for ellipses / ribbons. Default \code{0.95}.}
}
\value{
A \code{ggplot} object. If an internal error occurs, a simple red
"Plot error" placeholder plot is returned instead.
}
\description{
Thin wrapper around \code{\link[=kobe_all_in_one_gg]{kobe_all_in_one_gg()}} that traps errors and
returns a minimal error plot instead of failing. This is useful when you
batch-generate many Kobe plots and want the pipeline to continue even if
one fit is problematic.
}
\examples{
\dontrun{
p <- kobe_safe(S1_Pella.SDM, rel.axes = FALSE, CI = 0.95)
print(p)
}

}
\seealso{
\code{\link[=kobe_all_in_one_gg]{kobe_all_in_one_gg()}}, \code{\link[=build_kobe_01]{build_kobe_01()}},
\code{\link[=build_kobe_grids_scenario]{build_kobe_grids_scenario()}}
}
