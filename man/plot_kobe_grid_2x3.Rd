% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_kobe_grid_2x3.R
\name{plot_kobe_grid_2x3}
\alias{plot_kobe_grid_2x3}
\title{Unified 2×3 Kobe grid builder (single or multiple scenarios)}
\usage{
plot_kobe_grid_2x3(
  x,
  man.legend = FALSE,
  text_size = 4,
  save = TRUE,
  out_dir = "FIG/SS_PP",
  width = 20,
  height = 8,
  dpi = 300,
  file_basename = NULL,
  mode = c("auto", "single", "multi"),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{x}{Either:
\itemize{
\item A \strong{single scenario list} with up to six model fits named like
\code{SxP.SDM}, \code{SxS.SDM}, \code{SxF.SDM}, \code{SxP.GLM}, \code{SxS.GLM}, \code{SxF.GLM}, OR
\item A \strong{multi-scenario list}, where each element is such a single scenario list.
}}

\item{man.legend}{Logical; forwarded to \code{my_plot_kobe_all_management_scenario()}.}

\item{text_size}{Numeric; in-panel tag size.}

\item{save}{Logical; if TRUE, write PNG(s) to disk.}

\item{out_dir}{Character; parent output directory. For multi-scenario input,
a subfolder per scenario is created.}

\item{width, height}{Device size (inches) for saving.}

\item{dpi}{Resolution used by \code{ggsave()}.}

\item{file_basename}{Optional base name (without extension). If NULL, a stable
name is derived from the first entry (single scenario) or the scenario name (multi).}

\item{mode}{One of \code{"auto"}, \code{"single"}, \code{"multi"}.
\code{"auto"} attempts to infer based on structure.}

\item{verbose}{Logical; print progress and skips.}

\item{...}{Extra args forwarded to \code{my_plot_kobe_all_management_scenario()}.}
}
\value{
\itemize{
\item If single scenario: invisibly returns \code{list(plot = patchwork_obj, file = path_or_NULL)}.
\item If multi-scenario: invisibly returns a named list of such lists (one per scenario).
}
}
\description{
A single robust entry point that:
\itemize{
\item If given a \strong{single scenario list} (e.g., \code{list(S1P.SDM=..., S1S.SDM=..., ...)}),
builds \strong{one} 2×3 grid (rows = SDM/GLM; cols = P/S/F).
\item If given a \strong{multi-scenario list} (e.g., \code{list(S1=list(...), S2=list(...))}),
iterates scenarios and builds \strong{one} grid per scenario in subfolders.
}

All rendering is delegated to your existing
\code{my_plot_kobe_all_management_scenario()} (unchanged).

Each panel shows a small bold in-panel tag with the original list name
(e.g., "S1P.SDM"). Blank cells are rendered when a model is missing.
}
