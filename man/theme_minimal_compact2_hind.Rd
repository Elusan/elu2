% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_minimal_compact2_hind.R
\name{theme_minimal_compact2_hind}
\alias{theme_minimal_compact2_hind}
\title{Minimal, compact ggplot2 theme for SPiCT hindcast panels}
\usage{
theme_minimal_compact2_hind(base_size = 10, base_family = "")
}
\arguments{
\item{base_size}{Numeric. Base font size (points). Default \code{10}.}

\item{base_family}{Character. Base font family. Default \code{""}.}
}
\value{
A \code{\link[ggplot2:theme]{ggplot2::theme()}} object to be added to a plot.
}
\description{
A clean, compact variant of \code{\link[ggplot2:ggtheme]{ggplot2::theme_minimal()}} tailored for
SPiCT-style hindcast plots. It removes facet strip backgrounds \emph{and} strip
labels (the shaded header bar and any numeric strip text), keeps a crisp
panel border, and places a compact legend inside the panel.
}
\details{
Key choices:
\itemize{
\item \code{strip.background = element_blank()} and \code{strip.text = element_blank()} remove the shaded bar and its label.
\item Strong \code{panel.border} while \code{panel.grid} is removed.
\item Legend is inside the panel (\code{c(0.98, 0.98)}) with a compact white box.
}
}
\examples{
\dontrun{
library(ggplot2)
ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  facet_wrap(~ cyl) +
  theme_minimal_compact2_hind()
}

}
