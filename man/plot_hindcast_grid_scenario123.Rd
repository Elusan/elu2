% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_hindcast_grid_scenario123.r
\name{plot_hindcast_grid_scenario123}
\alias{plot_hindcast_grid_scenario123}
\title{Hindcast grid (2×3) for a single scenario (SDM vs GLM × Pella/Schaefer/Fox)}
\usage{
plot_hindcast_grid_scenario123(
  all_models,
  scenario,
  add.mase = TRUE,
  CI = 0.95,
  verbose = FALSE,
  show_legends = TRUE,
  title = NULL,
  npeels = 2L,
  peel.dtc = FALSE,
  save = TRUE,
  out_dir = file.path("FIG", "Hindcast"),
  width = 14,
  height = 6,
  dpi = 300
)
}
\arguments{
\item{all_models}{Named list-of-lists by scenario; e.g. \code{all_models$S1$S1P.SDM}.
Inside a scenario, expected names are: \code{SxP.SDM}, \code{SxS.SDM}, \code{SxF.SDM},
\code{SxP.GLM}, \code{SxS.GLM}, \code{SxF.GLM} (where \code{Sx} is the scenario name).}

\item{scenario}{Character; the scenario to plot (e.g., \code{"S1"}).}

\item{add.mase}{Logical; if \code{TRUE}, overlay MASE diagnostics when available.}

\item{CI}{Numeric scalar in (0,1); confidence level passed downstream.}

\item{verbose}{Logical; verbose messages from helpers.}

\item{show_legends}{Logical; keep per-panel legends (default \code{TRUE}).}

\item{title}{Ignored; kept for API compatibility (grid has no top title).}

\item{npeels}{Integer; hindcast peels (default \code{2L}).}

\item{peel.dtc}{Logical; data-time-coverage peeling (default \code{FALSE}).}

\item{save}{Logical; if \code{TRUE}, save a PNG next to returning the grid.}

\item{out_dir}{Output directory for PNG (created if needed).}

\item{width, height, dpi}{Graphics device parameters for saved PNG.}
}
\value{
A patchwork object representing the 2×3 grid. Invisibly saves a PNG
when \code{save = TRUE}.
}
\description{
Builds a 2×3 panel grid for one scenario. Columns are \strong{Pella}, \strong{Schaefer},
\strong{Fox}; top row is \strong{SDM}, bottom row is \strong{GLM}. Each panel calls
\code{plotspict.hindcast_elu2_gg_exact_FOR_GRIDS()} and adds a bold tag in the
top-left corner (e.g., \code{"S1P"}). Missing models or plot errors are shown as
placeholder panels with an informative label. The grid title is intentionally
omitted (panel tags carry the identification).
}
\examples{
\dontrun{
# Single scenario:
p <- plot_hindcast_grid_scenario123(
  all_models, scenario = "S1",
  npeels = 2L, peel.dtc = FALSE,
  add.mase = TRUE, CI = 0.95, verbose = FALSE,
  show_legends = TRUE, save = TRUE,
  out_dir = file.path("FIG", "Hindcast"),
  width = 14, height = 6, dpi = 300
)
}
}
\seealso{
spict::hindcast, plotspict.hindcast_elu2_gg_exact_FOR_GRIDS
}
