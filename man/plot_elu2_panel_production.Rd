% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_elu2_panel_production.R
\name{plot_elu2_panel_production}
\alias{plot_elu2_panel_production}
\title{Production curve (ggplot2) — theoretical P(B) + time-ordered trajectory}
\usage{
plot_elu2_panel_production(
  rep,
  n.plotyears = 40,
  main = "Production curve",
  stamp = get.version(),
  CI = 0.95
)
}
\arguments{
\item{rep}{A fitted SPiCT report object (\code{spictcls}).}

\item{n.plotyears}{Integer; if the number of trajectory points is less than
\code{n.plotyears}, year labels are drawn (first year is \strong{bold}).
Default \code{40}.}

\item{main}{Plot title. Default \code{"Production curve"}.}

\item{stamp}{Small stamp (e.g. version string) placed bottom-right.
Default \code{spict::get.version()}.}

\item{CI}{Confidence level (0,1) for parameter extraction. Default \code{0.95}.}
}
\value{
Invisibly returns the \code{ggplot} object after drawing it.
}
\description{
Draws the theoretical production curve \eqn{P(B)} from a fitted SPiCT model,
optionally overlaying the time-ordered production/biomass trajectory
(if \code{inp$reportall} is \code{TRUE}). When time-varying growth or log-m-covariance
is enabled, the y-axis is normalised against the maximum of the theoretical
curves (as in the base plotting logic).
}
\details{
Minimal equivalents of external helpers are used via \pkg{spict}:
\code{get.par()} and \code{get.version()}. A compact theme helper
is inlined (file-local) and applied at the end.
}
\examples{
\dontrun{
rep <- fit.spict(inp)
plot_elu2_panel_plotspict.production_gg(rep)
}

}
