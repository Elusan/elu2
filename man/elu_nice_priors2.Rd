% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elu_nice_priors2.R
\name{elu_nice_priors2}
\alias{elu_nice_priors2}
\title{Save Prior vs Posterior Plots for Multiple Models}
\usage{
elu_nice_priors2(
  models,
  priors_fun = priors.elu66,
  outdir = "FIG",
  width = 8,
  height = 4,
  dpi = 300
)
}
\arguments{
\item{models}{A named list of fitted model objects (e.g., results from \code{fit.elu2()} or similar).}

\item{priors_fun}{Function to plot priors and posteriors for a single model. Default is \code{priors.elu6}.}

\item{outdir}{Output directory to save PNG figures. Created if it does not exist. Default: "FIG".}

\item{width}{Width of the output PNG figure in inches. Default: 8.}

\item{height}{Height of the output PNG figure in inches. Default: 4.}

\item{dpi}{Resolution of the PNG figure in dots per inch. Default: 300.}
}
\value{
Invisibly returns \code{NULL}. Called for its side effect of saving plots.
}
\description{
Generates and saves prior vs posterior distribution plots (as PNG files) for each fitted model object in a list.
}
\details{
For each model in \code{models}, this function calls the specified \code{priors_fun} (typically a prior vs posterior plotting function)
and saves the resulting plot as a PNG in the output directory.

The PNG files are named \code{"priors_<model_name>.png"} according to the names in the input list.
}
\examples{
\dontrun{
models <- list(
  S1F = fit_fox,
  S1P = fit_pella,
  S1S = fit_schaefer
)
elu_nice_priors(models)
}
}
