% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sumspict.priors.df.R
\name{sumspict.priors.df}
\alias{sumspict.priors.df}
\title{Extract and Format Priors from SPiCT Model}
\usage{
sumspict.priors.df(rep, ndigits = 8)
}
\arguments{
\item{rep}{A fitted SPiCT model object, typically the result of \code{fit.spict()} or a custom equivalent
like \code{fit.elu2()}. This object must contain a valid \code{rep$inp$priors} and optionally \code{rep$inp$matrixpriors}.}

\item{ndigits}{Number of digits to use for rounding values in the output (currently not used directly in this function).}
}
\value{
A \code{data.frame} with columns:
\describe{
\item{Parameter}{The name of the parameter with an active prior.}
\item{Prior}{A string describing the prior distribution (e.g., \code{~ dnorm[log(0.7), 0.2^2]}).}
}
If no priors are used, the returned data frame will contain a single row stating \code{"No priors are used"}.
}
\description{
This function extracts the active prior distributions used in a fitted SPiCT model object
(including custom priors and random effects priors), and formats them into a data frame suitable
for LaTeX export or reporting. The output includes the parameter name and its associated prior distribution
in a readable mathematical format (e.g., \code{dnorm}, \code{dgamma}).
}
\details{
This function:
\itemize{
\item Identifies which priors are active using \code{priorsuseflag}.
\item Handles standard priors, matrix priors, and random effect priors (e.g., \code{logB}, \code{logF}).
\item Converts gamma priors to mean/sd format via \code{shaperate2meanvar}.
\item Outputs clean formatted strings that can be easily exported using \code{kable()} or \code{xtable()}.
}
}
\examples{
\dontrun{
  df_priors <- sumspict.priors.df(fit.spict(my_data))
  kableExtra::kbl(df_priors, format = "latex")
}

}
\seealso{
\code{\link{sumspict.priors}} for a console-printing version of this function.
}
