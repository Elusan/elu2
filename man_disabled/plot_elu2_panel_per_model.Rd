% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_elu2_panel_priors_per_model.R
\name{plot_elu2_panel_per_model}
\alias{plot_elu2_panel_per_model}
\title{Prior–posterior density (one model) — named ggplot or patchwork grid}
\usage{
plot_elu2_panel_per_model(
  rep,
  model_id = NULL,
  do.plot = NULL,
  stamp = get.version(),
  CI = 0.95
)
}
\arguments{
\item{rep}{A fitted SPiCT report object (\code{spictcls}).}

\item{model_id}{Optional model label embedded in messages; default tries \code{rep$name}.}

\item{do.plot}{Optional integer cap on number of panels. If \code{1}, returns the
single panel immediately. Default \code{NULL} (all active priors).}

\item{stamp}{Optional message (emitted when returning a grid).}

\item{CI}{Confidence level (0,1) for parameter extraction. Default \code{0.95}.}
}
\value{
If \code{do.plot = 1}, a single \code{ggplot}. Otherwise, a \code{patchwork}
object containing all panels (invisible).
}
\description{
Builds prior–posterior density panels for each \strong{active} prior in a fitted
SPiCT/ELU model. If \code{do.plot = 1}, returns a single ggplot panel (first active
prior) immediately; otherwise returns a patchwork grid of all active priors.
}
\examples{
\dontrun{
rep <- fit.spict(inp)
p1  <- plot_elu2_panel_priors.elu6_per_model(rep, do.plot = 1)
pA  <- plot_elu2_panel_priors.elu6_per_model(rep)
}

}
