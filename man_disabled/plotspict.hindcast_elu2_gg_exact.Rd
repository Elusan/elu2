% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotspict.hindcast_elu2_gg_exact.R
\name{plotspict.hindcast_elu2_gg_exact}
\alias{plotspict.hindcast_elu2_gg_exact}
\title{SPiCT hindcast plot (elu2 exact version)}
\usage{
plotspict.hindcast_elu2_gg_exact(
  rep,
  add.mase = TRUE,
  CI = 0.95,
  verbose = TRUE,
  xlim = NULL,
  ylim = NULL,
  xlab = "Year",
  ylab = NA,
  plot.log = TRUE,
  legend.title = NULL,
  legend.pos = "topright",
  legend.ncol = 1,
  asp = 2,
  stamp = get.version()
)
}
\arguments{
\item{rep}{A fitted SPiCT result object that contains \code{$hindcast}
output and can be consumed by \code{extract.hindcast.info()}.}

\item{add.mase}{Logical. If \code{TRUE}, annotate panels with MASE values. Default \code{TRUE}.}

\item{CI}{Numeric in (0,1). Confidence level for ribbons (e.g. \code{0.95}). Default \code{0.95}.}

\item{verbose}{Logical. Verbosity passed to \code{extract.hindcast.info()}. Default \code{TRUE}.}

\item{xlim}{Optional numeric length-2 vector for x-axis limits (passed to \code{coord_cartesian}).}

\item{ylim}{Optional numeric length-2 vector for y-axis limits (passed to \code{coord_cartesian}).}

\item{xlab}{Character axis label for x. Ignored at the end because the function
sets \code{x = "Year"} explicitly for SPiCT parity. Default \code{"Year"}.}

\item{ylab}{Character axis label for y. If \code{NA}, the function sets
\code{y = "log(Index)"} explicitly for SPiCT parity. Default \code{NA}.}

\item{plot.log}{Logical. If \code{FALSE}, exponentiate \code{obs/pred/lc/uc} before plotting.
Default \code{TRUE}.}

\item{legend.title}{Optional character legend title. Default \code{NULL} (no title).}

\item{legend.pos}{Ignored; legacy parameter retained for signature compatibility.}

\item{legend.ncol}{Ignored; legend layout is forced to two columns internally.}

\item{asp}{Numeric aspect parameter reserved for compatibility (not used here). Default \code{2}.}

\item{stamp}{Character caption (e.g., \code{get.version()}) shown at the bottom-right. Default \code{get.version()}.}
}
\value{
A \code{ggplot} object.
}
\description{
Produce SPiCT-style hindcast plots with ggplot2. This function draws the
reference fit (ribbon + line), colored peel prediction lines for each
hindcast peel, and a short \strong{dashed} grey tail segment for the final
prediction step of each peel. Observations are shown as large circles
(white pre-hindcast; colored during hindcast). The legend is arranged in
two columns: column 1 lists \emph{Ref} and peel labels; column 2 shows
only \emph{obs} and \emph{pred}.
}
\details{
\strong{Design choices}
\itemize{
\item The final segment of each peel is drawn with \code{linetype = "dashed"} and \code{color = "grey30"},
mimicking the base-graphics SPiCT style that highlights the evaluation step.
\item Legend uses two columns: \emph{Ref} + peel labels in column 1, and \emph{obs}, \emph{pred} in column 2 only.
\item Facet strip background and text are removed via \code{theme_minimal_compact2_hind()} to avoid shaded headers and numeric labels.
\item Axis titles are explicitly set to \code{"Year"} and \code{"log(Index)"} for consistency with earlier SPiCT plots.
}

\strong{Dependencies}
Requires \pkg{ggplot2}. Uses a helper \code{extract.hindcast.info()} that must be available
in your environment (e.g., provided by \pkg{spict} or your package).
}
\examples{
\dontrun{
# Suppose `rep_hc` is the result of a SPiCT hindcast:
# rep_hc <- hindcast(fit, npeels = 3, peel.dtc = FALSE)
p <- plotspict.hindcast_elu2_gg_exact(rep_hc, add.mase = TRUE, CI = 0.95, verbose = FALSE)
print(p)
}

}
\seealso{
\code{\link{theme_minimal_compact2_hind}}
}
