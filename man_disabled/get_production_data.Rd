% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_production_data.R
\name{get_production_data}
\alias{get_production_data}
\title{Extract Equilibrium Production Curve from SPiCT Model Fit}
\usage{
get_production_data(rep, model_name = "Model")
}
\arguments{
\item{rep}{A fitted SPiCT model object (usually the output of \code{fit.spict()} or a compatible function). Must contain model parameter estimates and their names.}

\item{model_name}{Character. Name for the model to be included in the returned data frame (default: "Model").}
}
\value{
A data frame with columns:
\describe{
\item{B_K}{Relative biomass (B/K).}
\item{Production}{Estimated equilibrium production at each biomass.}
\item{Model}{Model name, for use in faceting or coloring in plots.}
}
Returns \code{NULL} if the input is not compatible.
}
\description{
Computes and returns equilibrium surplus production values across a biomass gradient for a fitted SPiCT model (e.g., Fox, Schaefer, or Pella-Tomlinson) for plotting production curves.
}
\details{
For each fitted model, this function:
\itemize{
\item Retrieves the most recent estimated parameters: carrying capacity (K), intrinsic rate of increase (m), shape parameter (n), and scaling coefficient (\code{gamma}).
\item Computes the equilibrium production across a grid of relative biomass values (\code{B/K}), using the generic surplus production formula.
\item Returns a data frame suitable for use in ggplot2 or further aggregation.
}
If the input model object contains a \code{"sderr"} entry (indicating a stochastic or error model, rather than a deterministic fit), the function returns \code{NULL}.
}
